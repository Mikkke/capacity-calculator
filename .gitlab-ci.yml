image: node:lts

stages:
  - build
  - deploy

cache:
  paths:
    - node_modules/

build:
  stage: build
  script:
    - npm ci
    - npm run build
    - mv dist public
  artifacts:
    paths:
      - public
    expire_in: 1 hour

pages:
  stage: deploy
  script:
    - echo "Déploiement sur GitLab Pages"
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH